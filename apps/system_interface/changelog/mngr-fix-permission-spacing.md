- Fixed an oversized vertical gap in the chat progress view below a resolved permission-request card. When the user grants or denies a request, the approval message is hidden and its verdict is folded into the card, but the reply still opens a fresh turn section -- so the card ended one progress block and the continuing steps rendered in the next, leaving the full turn-separation gap plus the card's own trailing margin between them. The seam below a resolved permission card now matches the normal step-to-step spacing.
