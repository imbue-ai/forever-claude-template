- Fixed an oversized vertical gap in the chat progress view below a resolved permission-request card. When the user grants or denies a request, the approval message is hidden and its verdict is folded into the card, but the reply still opens a fresh turn section -- so the card (and the agent's "I've sent a permission request..." reply that follows it) ended one progress block while the continuing steps rendered in the next, leaving the full turn-separation gap between them. The seam after a resolved permission turn now matches the normal step-to-step spacing, whether or not the card is followed by that trailing reply.
