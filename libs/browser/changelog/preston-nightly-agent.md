Marked `test_http_cast_closes_a_failed_launch_name_terminally` as flaky (test-only change): under xdist-parallel full-suite load the websocket client can observe the TCP drop before parsing the close frame, reading close code 1005 (NO_STATUS_RCVD) instead of the server's 1008, and a close status that was never parsed cannot be recovered by any client-side wait. The test's assertion stays strict; the flaky mark retries it instead.
