- A fresh workspace created without Claude credentials no longer deadlocks into a dead "No events yet for this agent" chat. The bootstrap now adopts an initial chat agent left behind by an earlier partial create (recording its id for the welcome resend and marking creation done) instead of re-running `mngr create` into a duplicate-name collision on every boot. See `libs/bootstrap/changelog/preston-adopt-partial-create.md` for details.
