The Caretaker's first-night welcome and its tab indicator are more polished:

- The new-tab "blink" now lights up the entire clickable tab region (the whole tab flashes in the workspace accent color), instead of only the inner label.

- The welcome message is formatted for readability, with a bold heading for the greeting and for the two setup questions.

- The hidden `/caretaker-welcome` command that triggers the greeting is no longer shown in the chat (handled the same way the main chat hides its `/welcome` trigger), so the first thing you see is the welcome itself.
