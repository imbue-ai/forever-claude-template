Rename this repo from forever-claude-template to default-workspace-template: parent.toml URL, pyproject name (uv.lock regenerated), the fct-seed / fct-setup-system / fct-install-dependencies binaries become default-workspace-template-* (scripts/fct_seed.sh renamed to scripts/default_workspace_template_seed.sh), and all fct/FCT references in docs, skills, and comments. Applied mechanically by scripts/rename_template_repo.py in the mngr repo. The GitHub repo itself is already renamed; old URLs keep working via redirect.
